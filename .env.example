# .env.example — copy to .env and fill in real values.
# Never commit .env to version control.
# See cs4all-backend/AGENTS.md Section 4 and docs/AGENTS.md Section 6.

# ── Supabase ──────────────────────────────────────────────────────────────────
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=        # NEVER share with the frontend. Bypasses all RLS.

# ── Redis ─────────────────────────────────────────────────────────────────────
REDIS_URL=redis://localhost:6379/0

# ── Webhook Security ──────────────────────────────────────────────────────────
# Shared secret between this backend and the Supabase webhook configuration.
# Optional in Phase 2 (local dev); REQUIRED in production (Phase 3+).
WEBHOOK_SECRET=

# ── LLM APIs (Phase 3.5) ─────────────────────────────────────────────────────
# Set whichever provider you will use. Only one is required.
GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Override the default LLM model name (optional).
# Defaults: Gemini → "gemini-2.0-flash", OpenAI → "gpt-4o-mini"
LLM_MODEL=

# ── GitHub (Phase 3.5) ───────────────────────────────────────────────────────
# Read-only PAT scoped only to vietfood/cs4all-content, for rubric fetching.
GITHUB_TOKEN=

# ── Application ───────────────────────────────────────────────────────────────
ENVIRONMENT=development           # "development" | "production"
